# Real diabetes data never enters this repo.
# Snapshots live outside it and are referenced by path.
*.db
*.db-wal
*.db-shm
*.sqlite*
/data/
/out/
*.ndjson
*.ndjson.gz

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Rust
/target/
**/target/

# Android / Gradle
.gradle/
/build/
local.properties
*.apk

# Editors / OS
.DS_Store
.idea/
*.swp

# Rust spike build output
/spike/*/target/
/crates/*/target/
/crates/*/jniLibs/
/plugin/src/main/jniLibs/

# Gradle output for the AAPS module. Root-only `/build/` missed this, so
# every build committed 764 files including two ~20 MB .so binaries.
/plugin/build/
